import Panel from"../ui/Panel";import type{OperationalControl}from"../../services/operationalApi";
export default function ControlEffectiveness({data}:{data:OperationalControl[]|null}){return <Panel title="Control Effectiveness" subtitle="Enterprise internal control performance"><div className="space-y-6">{(data??[]).map(item=><div key={item.name}><div className="mb-2 flex justify-between"><span className="font-medium">{item.name}</span><span className="font-bold">{item.score.toFixed(1)}%</span></div><div className="h-3 rounded-full bg-slate-800"><div className={`h-3 rounded-full ${item.status==="Healthy"?"bg-emerald-500":item.status==="Watch"?"bg-amber-500":"bg-rose-500"}`} style={{width:`${Math.min(100,item.score)}%`}}/></div></div>)}</div></Panel>}
