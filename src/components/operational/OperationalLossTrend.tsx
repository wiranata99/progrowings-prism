import{Area,AreaChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis}from"recharts";import Panel from"../ui/Panel";
export default function OperationalLossTrend({data}:{data:Array<{reportingDate:string;grossLoss:number;recovery:number;netLoss:number}>|null}){const points=(data??[]).map(p=>({...p,month:new Date(p.reportingDate).toLocaleDateString("en-GB",{month:"short"})}));return <Panel title="Operational Loss Trend" subtitle="Six-month gross, recovery, and net loss movement"><div className="h-80"><ResponsiveContainer width="100%" height="100%"><AreaChart data={points}><defs><linearGradient id="lossGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#22d3ee" stopOpacity={.35}/><stop offset="95%" stopColor="#22d3ee" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" stroke="#334155"/><XAxis dataKey="month" stroke="#94a3b8"/><YAxis stroke="#94a3b8"/><Tooltip formatter={(value,name)=>[`Rp ${Number(value).toFixed(2)} B`,String(name)]}/><Area type="monotone" dataKey="grossLoss" name="Gross Loss" stroke="#22d3ee" strokeWidth={3} fill="url(#lossGradient)"/><Area type="monotone" dataKey="netLoss" name="Net Loss" stroke="#f59e0b" fillOpacity={0}/></AreaChart></ResponsiveContainer></div></Panel>}
