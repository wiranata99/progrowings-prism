import Panel from"../ui/Panel";import StatusBadge from"../ui/StatusBadge";import type{OperationalIssue}from"../../services/operationalApi";
const badge=(s:string):"success"|"warning"|"danger"=>s==="Mitigated"?"success":s==="Open"?"danger":"warning";
export default function TopOperationalIssues({data}:{data:OperationalIssue[]|null}){return <Panel title="Top Operational Issues" subtitle="Highest priority operational risks"><div className="overflow-x-auto"><table className="w-full"><thead><tr className="border-b border-slate-800 text-left text-xs uppercase tracking-[0.15em] text-slate-500"><th className="pb-4">Risk</th><th className="pb-4">Owner</th><th className="pb-4">Exposure</th><th className="pb-4">Due</th><th className="pb-4">Status</th></tr></thead><tbody>{(data??[]).map(item=><tr key={item.id} className="border-b border-slate-800 transition hover:bg-slate-800/40"><td className="py-5 font-medium">{item.risk}</td><td>{item.owner}</td><td className="font-semibold text-amber-400">Rp{item.exposure.toFixed(0)} B</td><td>{item.dueDays} Days</td><td><StatusBadge status={badge(item.status)} text={item.status}/></td></tr>)}</tbody></table></div></Panel>}
